#include "holberton.h"

/**
* mul - Multiplies two integers.
* @a: input number.
* @b: input number.
* Return: Result of a *b
*/
int mul(int a, int b)
{
int result;
result = a * b;
return (int result);
}
