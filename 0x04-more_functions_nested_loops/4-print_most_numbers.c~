#include "holberton.h"

/**
* print_numbers - Prints numbers between 0 to 9.
* Return: No return.
*/
void print_most_numbers(void)
{
int str[] = {48, 49, 51, 52, 53, 54, 55, 56, 57}
int count, sz;

sz = sizeof(str) / sizeof(int);
for (count = 0; count < sz; count++)
{
_putchar(str[count]);
}
_putchar('\n');
return (0);
}
